# Memorable Password Generator

A small, dependency-free password generator that creates passwords in this format:

`123-Red-Dog`

The generator runs entirely in the browser and does not transmit or store generated passwords.

## GitHub Pages

Publish from the repository's `main` branch and root (`/`) folder under **Settings → Pages**.

> This memorable format has limited combinations. Use a password manager for sensitive accounts.
